## What changes and why

<!-- Describe the change and the motivation behind it. Link the related issue if there is one. -->

## Checklist

- [ ] Tests were written before the implementation (TDD), following the `// Given` / `// When` / `// Then` structure
- [ ] `./gradlew build` passes locally (tests, ktlint, detekt)
- [ ] KDoc was added or updated for any new or changed public API
- [ ] The README was updated if this changes documented behavior or limitations
- [ ] Any new financial constant (PD, LGD, haircut...) is clearly documented as app-defined, not a real regulatory value
